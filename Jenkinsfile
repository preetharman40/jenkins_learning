pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application... (Imagine we are compiling code here)'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing the application... (Imagine we are running unit tests here)'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying to Production... (Imagine we are sending files to a server)'
            }
        }
    }
}